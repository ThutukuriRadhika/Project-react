import React from 'react';
import style from "./navbar.module.css"

const Button = () => {
    return(
        <div id={style.BtnBlock}>
            <button>Get Started</button>

        </div>
    )
}
export default Button